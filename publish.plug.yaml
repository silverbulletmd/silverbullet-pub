name: publish
assets:
  - "assets/*"
functions:
  # For use from the CLI
  publishAll:
    path: "./publish.ts:publishAll"

  publishAllCommand:
    path: "./publish.ts:publishAllCommand"
    command:
      name: "Publish: Publish All"
